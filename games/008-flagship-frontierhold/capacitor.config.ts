import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gametestscrapbuild.frontierhold008',
  appName: 'Frontierhold',
  webDir: 'dist'
};

export default config;
